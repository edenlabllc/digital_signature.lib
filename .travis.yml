language: elixir
cache:
  directories:
    - _build
    - deps
elixir:
  - 1.5.1
otp_release:
  - 20.0.4
env:
  global:
    - MIX_ENV=test
branches:
  except:
    - /[0-9]*\.[0-9]*\.[0-9]*/
script:
  # Run all tests except pending ones
  - ./bin/mix_tests.sh || travis_terminate 1
